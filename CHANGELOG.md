# 更新日志

本文件记录各版本的变更。版本号与 `com.dsh.aitranslator/CSXS/manifest.xml` 中的 `ExtensionBundleVersion` 保持一致。

---

## v1.0.0 — 2026-09-10

首个发布版本。

### 功能

- **画布文本批量翻译**：支持「整个文档 / 当前选中 / 当前画板 / 当前图层」四种范围，递归遍历组内文本
- **格式保真**：逐段替换并快照/恢复字符属性（字号、字体、填充色、字距、水平/垂直缩放、基线偏移）
- **联排文本框**：按 `nextFrame`/`previousFrame` 识别为一个故事整体翻译，回写时逐框倒序处理
- **溢出处理**：区域文本翻译变长后自动放大文本框（最多 3 倍）或缩小字号（下限 60%），也可只标记
- **输出大小写**：全部大写（大写英文 `SAVE AS`）/ 全部小写 / 单词首字母大写
- **撤销**：快照回滚，保留最近 30 次操作
- **离线模式**：导出 JSON → 外部翻译 → 导入写回（导入支持按 id 或按原文匹配）
- **术语表**：一行一条 `原文=译文`，翻译前预替换以保证译法一致

### 翻译引擎

免 Key 免费引擎（默认，按顺序自动降级）：

| 引擎 | 说明 |
|---|---|
| 腾讯交互翻译 | 支持批量，速度最快 |
| Bing 微软翻译 | 质量最高，服务端走 LLM；触发 IP 配额后自动冷却 5 分钟 |
| 有道翻译 | 响应最快；限流严格，已自动串行 |
| Google 免费接口 | 国内需代理（面板可自动检测系统代理） |
| MyMemory | 兜底，匿名每天 1000 词额度 |

需 Key 引擎（可选）：DeepSeek、智谱 GLM-4-Flash（有免费额度）、硅基流动、DeepL、本地 Ollama。

网络层特性：代理 CONNECT 隧道、直连/代理双通道自动回退、重试退避、并发节流、重定向与 Cookie 跟随。

### 若干实现要点

- 扫描时跨对象去重：10 个按钮都写「Save as」只请求一次
- 自动跳过：空文本、纯数字/符号、网址、邮箱、占位符、已是目标语言的文本
- 译文数量与请求不一致时抛错换引擎，杜绝错位回写

### 验证情况

共 **139 项断言全部通过**：

| 测试层 | 数量 | 内容 |
|---|---|---|
| 纯逻辑单测 | 35 | 切分、过滤、去重、分批、术语表、大小写变换、LLM 输出解析 |
| jsdom 面板 UI 测试 | 42 | 真实加载 index.html，假宿主 + 假网络，跑通扫描→翻译→回写→撤销 |
| 真实引擎联网测试 | 41 | 5 个免费引擎真实调用 + auto 降级链 + 代理探测 |
| Illustrator 真机端到端 | 21 | COM 驱动，建测试文档验证落盘行为 |

真机实测推翻了 3 个想当然的脚本 API 假设，均已规避：

1. `textFrame.overflows` **不存在**（AI 2025 为 `undefined`）→ 改用「可见行字符总数 < 全文长度」判断溢出
2. 联排框 `contents` 的 **getter 与 setter 都是框局部的** → 扫描时拼接各框，回写/撤销都逐框倒序
3. 未联排时 `nextFrame` **返回自身** → 遍历用「自环即停」终止，否则会绕成死循环

### 兼容性

- 宿主声明 `ILST [27.0,99.9]`，即 **Illustrator 2023(27.x) ~ 2025(29.x)** 及以后版本通用
- 基于 CEP（`RequiredRuntime CSXS 9.0`），AI 2023 对应 CSXS.11，2025 对应 CSXS.12
- 真机验证仅在 **Illustrator 2025 (29.8.1)** 上完成；2023/2024 依声明支持，可用包内 `自检.bat` 自行确认

### 已知限制

- Illustrator 脚本 API 不暴露 `overflows`，溢出判断基于行字符数推算，极端排版（大量空行）可能误判
- 同一段内混排多种样式时，逐段替换只能保留首字符样式（API 限制）
- 免费接口为第三方公开链路，随时可能调整；插件会自动降级
- Bing 约为 8 条/窗口 的 IP 配额，超限后返回空响应，插件会冷却并切换引擎

### 安装

下载 `ai-text-translator-v1.0.0.zip` 解压后双击 `安装.bat`，然后完全重启 Illustrator，
菜单：窗口 → 扩展功能 → AI 文本翻译。无需签名、无需管理员权限、无需联网。
